openenv-core>=0.2.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
requests>=2.31.0
python-dotenv>=1.0.0

# LLM providers — all three are safe to install together
together>=1.3.0          # Meta Llama via Together AI
groq>=0.9.0              # Groq (fast Llama inference) — currently active in .env
openai>=1.30.0           # OpenAI-compatible

# Data generation
faker>=33.1.0
